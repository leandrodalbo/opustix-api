spring:
  application:
    name: ticketera
  datasource:
    driverClassName: org.postgresql.Driver
    hikari:
      connection-timeout: 2000
      maximum-pool-size: 5
  flyway:
    enabled: true

  sql:
    init:
      mode: always

